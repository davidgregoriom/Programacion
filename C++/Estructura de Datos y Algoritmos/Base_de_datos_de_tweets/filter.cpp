include "filter.h"


/*
void Filter::filter_db(const BinaryTree &all, BinaryTree &hastagss, BinaryTree& tweets)
{
    all.for_each([&](const hashtags& hastags) {
            if (hastags.hastg_()) {
                tweets.push(hastags);
            }
            else {
                hastagss.push(hastags);
            }
        });
}

*/
