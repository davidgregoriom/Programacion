#include <iostream>
#include <memory>

using namespace std;

int main(){
  std::unique_ptr<std::string> pString;
  std::unique_ptr<int> pInt;

  int miEntero=6;
  int *PuntAentero;

  pString = std::make_unique<std::string>("hola");
  pInt = std::make_unique<int>(3);

  PuntAentero=&miEntero;
  //pInt=&miEntero; //No puede convertir un int* a std::unique_ptr
  pInt=std::make_unique<int>(miEntero); //Esto si podemos hacerlo

  return 0;
}
